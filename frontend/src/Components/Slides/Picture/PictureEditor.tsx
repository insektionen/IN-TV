import {BaseEditor, SlideEditorProps} from '../SlideEditor';

export const PictureEditor = (props: SlideEditorProps) => {
    return (
        <BaseEditor {...props}>
            <h2>Picture edit</h2>
        </BaseEditor>
    );
};